'use client'
import {FormEvent,useState} from 'react'
export default function CannaSocialCard(){const [flip,setFlip]=useState(false),[tilt,setTilt]=useState({x:0,y:0}),[open,setOpen]=useState(false),[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');const [form,setForm]=useState({name:'',email:'',favorite:'',why:''});const move=(e:React.MouseEvent<HTMLDivElement>)=>{const r=e.currentTarget.getBoundingClientRect();setTilt({x:((e.clientY-r.top)/r.height-.5)*-14,y:((e.clientX-r.left)/r.width-.5)*14})};const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/canna-card/apply',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to submit');setSent(true)}catch(e){setError(e instanceof Error?e.message:'Unable to submit')}finally{setBusy(false)}};return <section className="cs-card-section shell" id="canna-card"><div className="cs-card-copy"><p className="eyebrow">CANNA SOCIAL MEMBERSHIP</p><h2>More than a card.<br/><span>It’s membership.</span></h2><p className="muted">Preview the official Canna Social Card below. Applications are reviewed personally. Approved members receive their official downloadable card by email.</p><div className="cs-benefits"><span>✓ Community Member</span><span>✓ Partner benefits</span><span>✓ Canna Social events</span></div><button className="cs-save" onClick={()=>setOpen(true)}>Apply for your Canna Social Card →</button><p className="cs-note">Approval required. The preview cannot be downloaded or used as membership proof.</p></div><div className="cs-stage"><div className="cs-card-wrap" onMouseMove={move} onMouseLeave={()=>setTilt({x:0,y:0})} onClick={()=>setFlip(!flip)} style={{transform:`rotateX(${tilt.x}deg) rotateY(${tilt.y+(flip?180:0)}deg)`}}><div className="cs-card cs-front"><div className="cs-holo">✦</div><div className="cs-top"><b>CANNA SOCIAL</b><span>PREVIEW</span></div><div className="cs-logo">✦</div><div className="cs-name">COMMUNITY MEMBER</div><div className="cs-bottom"><span>CANNA SOCIAL</span><span>21+ • USA</span></div></div><div className="cs-card cs-back"><div><b>CANNA SOCIAL</b><p>THE PEOPLE’S CANNABIS PLATFORM</p></div><div className="cs-pattern">✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦</div><div className="cs-back-note">Official digital membership card for approved members. Partner benefits vary. Not a government ID.</div><div className="cs-bottom"><span>APPROVAL REQUIRED</span><span>SCAN • CONNECT • DISCOVER</span></div></div></div><small>CLICK / TAP TO FLIP · 3D PREVIEW</small></div>{open&&<div className="cs-modal-backdrop" onClick={()=>!busy&&setOpen(false)}><div className="cs-modal" onClick={e=>e.stopPropagation()}><button className="cs-close" onClick={()=>setOpen(false)}>×</button>{sent?<div className="cs-success"><div>✓</div><p className="eyebrow">APPLICATION RECEIVED</p><h3>You’re in the review queue.</h3><p>We’ll review your application. If approved, your official Canna Social Card will be emailed to the address you provided.</p><button className="cs-save" onClick={()=>setOpen(false)}>Back to Canna Social</button></div>:<><p className="eyebrow">CANNA SOCIAL CARD APPLICATION</p><h3>Apply for membership.</h3><p className="muted">Give us a few details. Approval is required before the official card is issued.</p><form onSubmit={submit}><input required placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required type="email" placeholder="Email for your card" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input placeholder="Favorite strain or cannabis category" value={form.favorite} onChange={e=>setForm({...form,favorite:e.target.value})}/><textarea required rows={4} placeholder="Why do you want to join Canna Social?" value={form.why} onChange={e=>setForm({...form,why:e.target.value})}/><label className="cs-check"><input required type="checkbox"/> I confirm I am 21+ and understand approval is required.</label>{error&&<p className="form-error">{error}</p>}<button className="cs-save wide" disabled={busy}>{busy?'Submitting…':'Submit application →'}</button></form></>}</div></div>}</section>}
